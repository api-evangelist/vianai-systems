generated: '2026-07-21'
method: derived
status: candidate
source: https://docs.vian.ai/5.0-r2/api-list.html
notes: No official hosted/remote MCP server was found for Vianai/hila. This is a CANDIDATE tool list derived
  from the publicly documented hila REST endpoints (method + path from the api-list). Tools map 1:1 to
  real endpoints; request/response schemas are served by the per-deployment OpenAPI (/docs) and are not
  reproduced here.
server:
  name: hila
  transport: http
tools:
- name: login
  description: Obtain a bearer access_token from username/password.
  source_operation: POST /v1/user/login
- name: search_projects
  description: List/search projects in the stack.
  source_operation: POST /v1/projects/search
- name: search_models
  description: List/search registered models.
  source_operation: POST /v1/models/search
- name: create_registered_model
  description: Register a new model.
  source_operation: POST /v1/model_deployment/createRegisteredModel
- name: register_model_version
  description: Register a new version of a model.
  source_operation: POST /v1/model_deployment/registerModelVersion
- name: create_model_deployment
  description: Deploy a registered model version.
  source_operation: POST /v1/model_deployment/createModelDeployment
- name: submit_drift_detection
  description: Submit a drift-detection job; returns a job_id.
  source_operation: POST /v1/driftdetection/submit
- name: drift_detection_status
  description: Poll drift-detection job status.
  source_operation: GET /v1/driftdetection/status/{job_id}
- name: submit_model_performance
  description: Submit a model-performance evaluation job.
  source_operation: POST /v1/modelperformance/submit
- name: model_performance_status
  description: Poll model-performance job status.
  source_operation: GET /v1/modelperformance/status/{job_id}
- name: submit_explainability
  description: Submit an explainability job.
  source_operation: POST /v1/explainability/submit
- name: submit_alert
  description: Submit/run a monitoring alert.
  source_operation: POST /v1/alerts/submit
- name: submit_data_profiling
  description: Submit a data-profiling job.
  source_operation: POST /v1/dataprofiling/submit
- name: track_inference
  description: Record inference tracking data.
  source_operation: POST /v1/inference-tracking
deployment:
  mode: none
  verified: derived
  tools: 14
  checked: '2026-08-12'
  source: catalog MCP census
